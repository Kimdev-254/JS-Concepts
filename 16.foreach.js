// foreach method for arrays

const numbers = [1, 2, 3, 4, 5];
// Using forEach to iterate over the array
numbers.forEach((number) => {
  console.log(number * 2); // Output each number multiplied by 2
}); // 2, 4, 6, 8, 10
